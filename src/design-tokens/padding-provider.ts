import { StyleSheet } from "react-native";

const paddFromScreenEdges = StyleSheet.create({
  pad: {
    paddingLeft: 8,
    paddingRight: 8,
    paddingTop: 20,
  },
});

export default paddFromScreenEdges;
